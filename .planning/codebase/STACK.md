# 技术栈

> Generated by GSD `map` on 2026-06-28. Reflects actual files on disk.

## 项目性质

**非应用运行时** — 这是一套 **skill + 知识 + 数据 的本地工作流系统**,没有可执行的代码运行时,只有:
- Markdown 文档(skill 定义 + 知识库)
- YAML/TSV 数据文件(用户画像 + 投递追踪)
- PowerShell 脚本(审计/冒烟测试)

## 文件类型构成

| 类型 | 数量 | 用途 |
|------|------|------|
| `.md` | ~75 | skill 定义 / 知识库 / 文档 / 规划 |
| `.yml` | 2 | 用户画像(profile.yml + 脱敏模板) |
| `.ps1` | 2 | 审计脚本 |
| `.tsv` | 1 | 投递追踪表 |
| `.json` | 1 | GSD 配置 |
| `.gitignore` | 1 | 隐私保护 |

## Skill 定义格式

| 维度 | 实现 |
|------|------|
| frontmatter | YAML(name/displayName/version/agent_created/description/trigger) |
| 正文 | Markdown(角色 → 运行规则 → 4 模块 → 输出规范 → 参考文档) |
| 运行时依赖 | 无(纯文本,由 AI agent 解释执行) |

## 数据格式

| 文件 | 格式 | schema 来源 |
|------|------|------------|
| `config/profile.yml` | YAML | 字段自描述;`profile.example.yml` 为脱敏模板 |
| `data/tracker.tsv` | TSV(8 列) | `skills/career-app-tracker/references/tracker-spec.md` |
| `templates/cv-template.md` | Markdown + `{{占位符}}` | 自描述 |

## 脚本运行时

| 脚本 | 语言 | 依赖 |
|------|------|------|
| `scripts/audit.ps1` | PowerShell 5.1+ / pwsh 7+ | 无外部模块 |
| `scripts/smoke-test.ps1` | PowerShell 5.1+ / pwsh 7+ | 无外部模块 |

> 脚本设计为零依赖(纯 PowerShell 内置 cmdlet),便于在任何 Windows/跨平台环境运行。

## 外部依赖(skill 运行时声明,非项目自身代码)

skill 正文声明调用以下外部能力(运行时由 AI agent 提供):
- **WebSearch** — 岗位搜索、行业调研、薪资刷新
- **WebFetch** — JD 页面、公司官网
- **企查查/天眼查 MCP** — 公司背调(可选,未连接则降级提示)

## 无构建/打包/测试框架

项目**不需要** package.json / requirements.txt / Makefile / CI。
验证手段是两个 PowerShell 脚本:`audit.ps1` + `smoke-test.ps1`。
