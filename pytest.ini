[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q --tb=short
markers =
    unit: 纯函数或无真实库 IO
    integration: 临时 SQLite / 打包 / 编排器闭环
    regression: 契约不变量与既有冒烟脚本
